name: Shorts Automation Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run test
        run: |
          echo "YouTube Shorts Automation is working!"
          echo "GitHub Actions test successful!"
